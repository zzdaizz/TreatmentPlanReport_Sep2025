<UserControl x:Class="TreamentPlanReport.Views.FieldSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TreamentPlanReport.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Select a Field" FontWeight="Bold" />
            <ListView ItemsSource="{Binding Fields}" SelectedItem="{Binding SelectedField}" DisplayMemberPath="FieldID"/>
        </StackPanel>
            
    </Grid>
</UserControl>
