<UserControl x:Class="TreamentPlanReport.Views.PatientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TreamentPlanReport.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Black" BorderThickness="0,0,0,1"/>
        <StackPanel Margin="5">
            <TextBlock Text="Patient Id" FontWeight="Bold"/>
            <TextBlock Text="{Binding PatientId}"/>
        </StackPanel>

        <!-- Last Name-->
        <Border BorderBrush="Black" BorderThickness="0,0,0,1" Grid.Column="1"/>
        <StackPanel Margin="5" Grid.Column="1">
            <TextBlock Text="Last Name" FontWeight="Bold"/>
            <TextBlock Text="{Binding PatientLastName}"/>
        </StackPanel>
        
        <!-- First Name-->
        <Border BorderBrush="Black" BorderThickness="0,0,0,1" Grid.Column="2"/>
        <StackPanel Margin="5" Grid.Column="2">
            <TextBlock Text="First Name" FontWeight="Bold"/>
            <TextBlock Text="{Binding PatientFirstName}"/>
        </StackPanel>

        <!-- DOB-->
        <Border BorderBrush="Black" BorderThickness="0,0,0,1" Grid.Row="1" Grid.Column="0"/>
        <StackPanel Margin="5" Grid.Row="1" Grid.Column="0">
            <TextBlock Text="DOB" FontWeight="Bold"/>
            <TextBlock Text="{Binding DateOfBirth}"/>
        </StackPanel>

        <!-- Hospital-->
        <Border BorderBrush="Black" BorderThickness="0,0,0,1" Grid.Row="1" Grid.Column="1"/>
        <StackPanel Margin="5" Grid.Row="1" Grid.Column="1">
            <TextBlock Text="Hospital" FontWeight="Bold"/>
            <TextBlock Text="{Binding Hospital}"/>
        </StackPanel>

        <!-- Oncologist-->
        <Border BorderBrush="Black" BorderThickness="0,0,0,1" Grid.Row="1" Grid.Column="2"/>
        <StackPanel Margin="5" Grid.Row="1" Grid.Column="2">
            <TextBlock Text="Oncologist" FontWeight="Bold"/>
            <TextBlock Text="{Binding PrimaryOncologist}"/>
        </StackPanel>

    </Grid>
</UserControl>
